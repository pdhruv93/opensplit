# OpenSplit API URL that the MCP server connects to
OPENSPLIT_BASE_URL=http://localhost:3000

# Port for the MCP HTTP server
OPENSPLIT_MCP_PORT=3001

# API key for stdio mode (not needed for HTTP mode)
# OPENSPLIT_API_KEY=
